export SPRING_PROFILES_ACTIVE=dev
export SPRING_DATASOURCE_URL="jdbc:postgresql://localhost:5432/trader"
export SPRING_DATASOURCE_USERNAME="trader"
# Local-dev credential only. PROD-REQUIRED secrets must come from a secret manager.
export SPRING_DATASOURCE_PASSWORD="trader"
export SERVER_PORT=8080

export MARKETDATA_HEALTH_SYMBOL="SPY"
# PROD-REQUIRED: replace placeholder before going live (validator will fail in prod).
export TRADING_API_KEY="local-dev-key"
